# Fixtures

**Synthetic only.** Generated by a committed, seeded script — see `docs/22-fixtures.md`.

There is no ingestion path by which real business data enters this repository. Costs,
hour counts, and fee amounts in specification fixtures are illustrative and are not
represented as current published schedules.

The generator is committed because a fixture set nobody can reproduce is not evidence of
anything.
